// my_design — 自定义 UI 组件（预留）
//
// 后续设计工具专用组件将放在这里：
// - DesignPreviewCard  (设计稿预览卡片)
// - SkillSelector      (风格选择面板)
// - TokenEditor        (Design Token 编辑器)
// - ExportPanel        (导出选项面板)

use gpui::*;
use gpui::prelude::FluentBuilder as _;

use crate::widgets::{h_flex, v_flex};
